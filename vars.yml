---
project_name: final_project

server_name: 54.226.198.110

install_root: /drizzlo

wsgi_module: final_project.wsgi

project_repo: https://github.com/joegotflow83/drizzlo

static_root: "{{ install_root }}/{{ project_name }}/static"

db_name: mydb
db_user: joe
db_password: admin123

system_packages:
    - git
    - nginx
    - gunicorn
    - postgresql
    - postgresql-server-dev-all
    - python3
    - python-pip
    - python-dev
    - python-psycopg2
